'https://py.checkio.org/en/mission/boundary-blocks/'

'''
ou are given a list of strings as grid. Empty cells will be represented by '.', and the blocks - by 'X'. 
Blocks are not adjacent to each other vertically and horizontally.

One area consists of empty cells that are adjacent to each other vertically and horizontally. 
You must return the coordinates of the blocks that make the boundary.

Input: A list of strings.

Output: An iterable of tuples of two integers (In row, column order).
'''

